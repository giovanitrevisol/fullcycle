mysql:
  username: ${DATABASE_MYSQL_USERNAME:root}
  password: ${DATABASE_MYSQL_PASSWORD:123456}
  schema: adm_videos
  url: ${DATABASE_MYSQL_URL:localhost:3306}
# Ao usar ${X} estamos buscando a variavel de ambiente
# mas podemos tambem usar valores padrão caso a variavel de ambiente não seja encontrada.
# ${DATABASE_MYSQL_USERNAME:root} -> ${VAR_AMBIENTE:valor-padrao}
#
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none